DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',  # or 'django.db.backends.postgresql', 'django.db.backends.sqlite3', etc.
        'NAME': 'sih',
        'USER': 'root',
        'PASSWORD': '12345',
        'HOST': 'AADVIK-PC',  # Or an IP address for remote databases
        'PORT': '3306',  # Default MySQL port; PostgreSQL typically uses 5432
    }
}